{
  "name": "filora-media",
  "version": "1.0.0",
  "private": true,
  "description": "Filora Media (FM) - hair styling content platform. Premium editorial studio aesthetic. No self-hosted media; all media is referenced via YouTube / Telegram / Google Drive links.",
  "type": "commonjs",
  "scripts": {
    "install:all": "npm install && npm --prefix client install",
    "setup": "npm run install:all && npm run db:seed",
    "db:seed": "node server/seed.js",
    "dev:server": "node server/index.js",
    "dev:client": "npm --prefix client run dev",
    "dev": "node server/scripts/dev.js",
    "build": "npm --prefix client run build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "start:prod": "npm run build && npm run start"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
